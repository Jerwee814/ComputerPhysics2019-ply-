#include <cmath>
#include <iostream>
using namespace std;

//f(x)=x^6
double power_6(double x) {
	return (x * x*x*x*x*x);
}
//f(x)=x^7
double power_5(double x) {
	return (x * x*x*x*x);
}
//f(x)=??
